# Environment Configuration
NODE_ENV=production
APP_PORT=4000
APP_HOST=0.0.0.0
APP_URL=https://api.thealley2b.pl
ALLOWED_ORIGINS=https://rezerwacje.thealley2b.pl

# Database Configuration
# For MariaDB/MySQL (both use the same connection string format)
DATABASE_URL=mysql://user:password@localhost:3306/reservation_system

# Note: All other settings (pricing, business hours, resources, etc.) are now managed
# through the admin panel and stored in the app_settings database table.

# Security Configuration
TURNSTILE_SECRET_KEY=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=


